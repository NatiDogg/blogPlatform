model User{
    id String @id @default(uuid())
    name String
    email String @unique
    password String
    role Role @default(USER)
    author Author?
    admin Admin?

}


enum Role {
    USER
    AUTHOR
    ADMiN

}